import tkinter

window = tkinter.Tk()
window.title("convertion km to m")
window.minsize(600, 600)

heading = tkinter.Label(text="Convert km to m GUI", font=("Arial", 24, "bold"))
heading.pack()


window.mainloop()